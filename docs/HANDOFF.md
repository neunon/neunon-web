# 引き継ぎメモ

このリポジトリの実装は完了している。ここから先は、Render のアカウントを
持っている側（坂本）が公開作業を進める。

---

## 1. いまどこまで終わっているか

要件定義書 13. の推奨順序に対して、**ステップ1〜8が完了**している。

| # | 内容 | 状態 |
|---|---|---|
| 1 | プロジェクト初期化・デザイントークン | 完了 |
| 2 | 共通レイアウト（ヘッダー／フッター／目次） | 完了 |
| 3 | トップページ | 完了 |
| 4 | 会社概要・事業詳細 | 完了 |
| 5 | 実績・人材パネル | 完了 |
| 6 | 採用サイト・求人票 | 完了 |
| 7 | フォーム | 完了 |
| 8 | SEO・アクセシビリティ・パフォーマンス | 完了 |
| 9 | **デプロイ** | **リポジトリ側の準備のみ完了。接続作業が残っている** |

全45ページが表示でき、内部リンク切れ 0 件、静的検査の指摘 0 件。
ローカルでの動作は確認済み。

---

## 2. Render に繋ぐには、GitHub のアクセス権が要る

このリポジトリは **private** なので、Render から接続するには、
**Render を操作する人の GitHub アカウントに、このリポジトリへのアクセス権が必要**。

> サイトを「見るだけ」なら GitHub は一切不要（公開後のURLを知っていれば誰でも見られる）。
> 必要になるのは「デプロイする」場合だけ。

進め方は2つある。

### 案A: コラボレーターとして招待する（すぐできる）

現在の所有者（`shn51020-max`）が、GitHub の
**Settings → Collaborators** から坂本の GitHub アカウントを招待する。
招待を受けたら、坂本が自分の Render アカウントから Blueprint で接続する。

- 手間が少ない
- リポジトリの所有者は `shn51020-max` のまま

### 案B: `neunon` organization へ移す（長期的にはこちら）

サイトは会社の資産なので、個人アカウントではなく organization に置く方が
担当者が変わっても引き継ぎやすい。すでに `neunon` organization が存在し、
`neunon/neunon-web` というリポジトリもある。

- GitHub の **Settings → Danger Zone → Transfer ownership** で移せる
- 移設後も URL のリダイレクトは効くが、Render の接続はやり直しになる
- **Render に繋ぐ前に決めた方がよい**（繋いだ後に移すと再接続が必要）

どちらにするかは坂本の判断。急ぐなら案A、落ち着いて整えるなら案B。

---

## 3. 坂本にやってもらうこと

順番に。詳細は [DEPLOY.md](DEPLOY.md) にある。

1. **上の案A/案Bを決めて、GitHub のアクセスを用意する**
2. **Render に接続する**（New → Blueprint。`render.yaml` を置いてあるので設定は自動で読まれる）
3. **環境変数を入れる**
   - `NEXT_PUBLIC_SITE_URL` … 最初は Render が発行する `*.onrender.com` のURLでよい
   - フォームの2つは、Formspree でフォームを作ってから
4. **初回デプロイ直後の3点を確認する**（DEPLOY.md 4章）
   - `/about/` が表示されるか
   - 存在しないURLで404ページが出るか
   - `/_next/static/` 配下に Cache-Control が付いているか
5. **キーボードだけでサイトを一周する**（DEPLOY.md 5.2）
   - 自動チェックでは代替できない。必ず一度は人の手で
6. **公開前の判断が必要な項目を片付ける**（README の「発注者への確認事項」）

---

## 4. 公開を止めている項目

これが揃わないと正式公開できない。

| 項目 | 必要なこと |
|---|---|
| プライバシーポリシー | リーガルチェック。保管期間などの決定 |
| サイト利用規約 | リーガルチェック |
| フォームの送信先 | Formspree でフォーム2つ作成、通知先アドレスの決定 |

以下は**未確定のままでも公開できる**。準備中と明示し、検索結果にも出ないようにしてある。

- 代表メッセージの原稿
- 沿革に載せる出来事
- お知らせの記事（現在はダミー3件）
- 人材パネルのデータ（現在は全件ダミー。実データ化には本人の掲載同意が前提）
- メンバーインタビュー
- 求人票の契約形態・報酬額

---

## 5. 内容を直したいとき

コードを触らなくても、`content/` 配下の JSON を書き換えれば直せるようにしてある。

| 直したいもの | ファイル |
|---|---|
| 事業の説明・メニュー・料金・FAQ | `content/services/*.json` |
| 支援実績 | `content/works/*.json` |
| 人材パネル | `content/talent/talents.json` |
| 求人票 | `content/jobs/*.json` |
| お知らせ | `content/news/*.json` |
| 会社情報・電話番号・住所 | `lib/site.ts` |

事業を4つ目に増やす場合も、`content/services/` に JSON を1枚足すだけで
トップページ・一覧・詳細ページ・sitemap のすべてに反映される。

公開後は、GitHub に push すると Render が自動でビルドし直す。
ビルド時に静的検査が走り、問題があればデプロイが止まる仕組みにしてある。

---

## 6. 引き継ぎ時に伝えておくべきこと

- **`neunon-logo.png`（リポジトリ直下）は受領した元データ**。加工前の状態で残してある
- **`neunon-site-requirements.md` が仕様の正**。実装の判断はすべてこれに紐づけてある
- 各ページに出ている「実装メモ」の枠は、**公開前に削除する**もの
- 要件定義書と実装が食い違っている箇所は、README の「発注者への確認事項」に
  番号を振って全部書き出してある（現在26項目、うち解決済み2件）
