# 作業の引き継ぎ（Codex ⇄ Claude Code）

> **エージェントへ:** セッション開始時に必ずここを読む。終了時に必ずここを更新して push する。
> 追記ではなく**上書き**する。長い履歴は残さない。最新の状態だけが価値を持つ。
> 書き方は `AGENTS.md`「二人体制のルール」を参照。

最終更新: 2026-09-25 / 更新者: Claude Code

---

## 1. いま進行中のこと

**コンサルティング事業ページの刷新（実装は完了、レビュー待ち）**

作業ブランチ: `hp/consulting-page`（push 済み・未マージ）

「コンサルティング事業_紹介資料.pptx」(2026-09-25 受領、54枚) のスライド5〜15に沿って
`/services/consulting/` を全面的に作り直した。マージは人間（坂本）の判断待ち。

| ファイル | 変更 |
|---|---|
| `content/services/01-consulting.json` | 資料の内容へ全面差し替え。`consulting` 拡張ブロックを追加 |
| `components/services/ConsultingServiceDetail.tsx` | 新規。パッケージ型支援と同じ「専用コンポーネント」方式 |
| `app/consulting.css` | 新規。globals.css の最後に読み込む |
| `app/services/[id]/page.tsx` | consulting を専用コンポーネントへ分岐。旧ハードコード節を削除 |
| `lib/content.ts` | `ConsultingDetail` 型を追加（Service では任意フィールド） |
| `scripts/audit-content.mjs` | 新ブロックの検証を追加。欠損はビルドで落ちる |
| `app/pages.css` / `app/globals.css` | 旧 `.nc-consulting-intro` 系の死んだCSSを撤去 |

ページ構成: 目指す姿 → 問題解決の考え方 → 主な支援テーマ → 支援実績 → 支援事例
→ 成果物イメージ → 支援形態 → なぜこの価格か → 進め方 → 想定期間・体制 → よくある質問

検証済み: typecheck / build / audit:content / audit:html すべて指摘なし。
dev サーバーで実ページを確認し、デスクトップ・375px とも横はみ出しなし、
目次アンカー11本すべて解決、コンソールエラーなし。ホームと他2事業ページも表示確認済み。

## 2. 次にやること

1. **`hp/consulting-page` のレビューとマージ判断**（人間）。下の「5. 確認が必要なこと」を先に потвердить
2. パッケージ型支援・AIプロダクトの2ページも、同じ紹介資料の体裁に揃えるか検討する
   （資料スライド17〜28にパッケージ型支援の事例・成果物サンプルがある）
3. 各ページに残っている「実装メモ」の枠を公開前に削除する
4. `README.md` の「発注者への確認事項」（27項目・うち解決済み4件）の棚卸し

## 3. 決まったこと（勝手に覆さない）

- 本番は Render Static Site。`origin/main` への push で自動デプロイされる
- 既存 URL の構造は変えない。末尾スラッシュ統一を維持する
- 新規 LP・記事の量産、分析ツール導入は今回のスコープ外
- 人材パネルは Microsoft Lists 連携。「サイト掲載可」の運用を守る
- プライバシーポリシー／利用規約はリーガルチェック待ち。中身を書き換えない
- **事業詳細ページは「専用コンポーネント + content JSON」方式で作る**（2026-09-25 決定）。
  汎用テンプレートに分岐を足していくのではなく、事業ごとにコンポーネントを分ける。
  文言修正が JSON だけで完結する状態を保つこと

## 4. 触ってはいけないもの・保留中

| 対象 | 理由 |
|---|---|
| 法務文書の本文 | リーガルチェック待ち |
| `neunon-logo.png` | 受領した元データ。加工前で保持 |
| CMS認証まわりの設定値 | GitHub OAuth App の初回設定が人間側で未完了 |
| 代表メッセージ・沿革・メンバーインタビュー・求人票の条件 | 原稿／条件が未確定。noindex で待機中 |

## 5. 確認が必要なこと（マージ前に人間の判断がいる）

1. **紹介資料の事例①②のタイトルが本文と入れ違っている。** スライド10の本文は
   応募活性化ドライバー分析、スライド11の本文は評価制度設計だが、タイトルが逆。
   サイトでは本文に合わせて正しい組み合わせにした。**資料側の修正も必要**
2. **「約50件」という実績数値をサイトに掲載した。** 要件定義書12.1 が禁じるのは
   取引先の実名と案件ごとの具体的数値なので抵触しないと判断したが、公開可否の最終確認を
3. **支援形態にパートナー（中小規模ファーム・個人コンサルタント）連携を明記した。**
   これまでサイトに無かった情報。公開してよいか確認を
4. SEOのtitle/descriptionを資料の支援テーマに合わせて書き換えた。
   既存URLは変えていないが、検索結果の表示文言は変わる

## 6. ハマったこと・共有事項

- `markitdown` はこの環境に入っていない。pptx の読み取りは `python-pptx` を使う。
  出力は必ず UTF-8 指定で（Windows の既定 cp932 で UnicodeEncodeError になる）
- スライドの図形はz順で返るため、ラベルと本文の対応は**座標（top/left）で並べ直して**確認する。
  テキスト抽出順をそのまま信じると対応がずれる
- Claude Code のプレビュー機能（launch.json）は、セッション途中でフォルダを切り替えると
  旧フォルダを見に行く。`npm run dev` を自分で起動して `navigate` する方が確実
- リモートブランチ `origin/codex/publish-readiness` と `origin/seo-metadata` は
  すでに `main` へ取り込み済みの残骸。参照しない
- ローカルの `main` は古いことがある。作業前に必ず `git fetch origin` する

---

## 更新のしかた

セッションを終えるとき（または上限が近いとき）に、1〜6 を今の状態に書き直す。
特に **1 と 2 は必ず埋める**。ここが空だと、次に入った側は何も分からない。

```bash
git add -A && git commit -m "作業ログを更新" && git push
```
